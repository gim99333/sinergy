number = int(input("¬ведите п€тизначное число: "))

# –азбиваем число на цифры
tens_of_thousands = number // 10000  # дес€тки тыс€ч
thousands = (number // 1000) % 10    # тыс€чи
hundreds = (number // 100) % 10      # сотни
tens = (number // 10) % 10           # дес€тки
units = number % 10                   # единицы

# ¬ыполн€ем вычислени€
step1 = tens ** units                # дес€тки в степени единиц
step2 = step1 * hundreds             # умножаем на сотни
denominator = tens_of_thousands - thousands  # разность дес.тыс. и тыс€ч

if denominator == 0:
    print("ќшибка: деление на ноль (разность дес€тков тыс€ч и тыс€ч равна нулю)")
else:
    result = step2 / denominator
    print(f"–езультат: {result}")
